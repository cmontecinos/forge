---
phase: 05-web-stack-base
plan: 02
type: execute
---

<objective>
Add Next.js frontend to the web stack template.

Purpose: Complete the web template with a Next.js App Router frontend that follows modern patterns.
Output: frontend/ directory in template with Next.js + Tailwind + TypeScript setup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context:
@.planning/phases/05-web-stack-base/05-01-SUMMARY.md

**Tech stack for frontend:**
- Next.js 14+ with App Router
- Tailwind CSS
- TypeScript

**Constraining decisions:**
- [PROJECT.md]: Next.js App Router (not Pages Router)
- [PROJECT.md]: Frontend → Go → Supabase (never direct connection)
- [PROJECT.md]: Tailwind CSS for styling

**Current state:**
Template repo exists at github.com/bigbytes/forge-template-web with backend/ structure.
This plan adds the frontend/ structure.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js frontend structure in template</name>
  <files>
    Template repo frontend/:
    - frontend/src/app/layout.tsx
    - frontend/src/app/page.tsx
    - frontend/src/app/globals.css
    - frontend/src/lib/api.ts
    - frontend/package.json
    - frontend/tsconfig.json
    - frontend/tailwind.config.ts
    - frontend/postcss.config.js
    - frontend/next.config.js
    - frontend/.env.example
  </files>
  <action>
    Clone the template repo locally and add frontend structure:

    1. Create frontend/package.json:
       - name: "{{.ProjectName}}-frontend" (or just "frontend")
       - dependencies: next, react, react-dom
       - devDependencies: typescript, @types/react, @types/node, tailwindcss, postcss, autoprefixer
       - scripts: dev, build, start, lint

    2. Create frontend/src/app/layout.tsx:
       - Root layout with html, body
       - Import globals.css
       - Use {{.ProjectName}} in metadata title

    3. Create frontend/src/app/page.tsx:
       - Simple home page
       - Display project name: {{.ProjectName}}
       - Link to backend health check

    4. Create frontend/src/app/globals.css:
       - Tailwind directives (@tailwind base, components, utilities)

    5. Create frontend/src/lib/api.ts:
       - API client that calls backend
       - Base URL from NEXT_PUBLIC_API_URL
       - Health check function

    6. Create frontend/tsconfig.json:
       - Standard Next.js TypeScript config
       - Path aliases (@/*)

    7. Create frontend/tailwind.config.ts:
       - Content paths for src/
       - Default theme

    8. Create frontend/postcss.config.js:
       - Tailwind and autoprefixer plugins

    9. Create frontend/next.config.js:
       - Basic Next.js config

    10. Create frontend/.env.example:
        - NEXT_PUBLIC_API_URL=http://localhost:8080

    Use text/template syntax for variables.
  </action>
  <verify>
    - All frontend files created
    - Template variables present ({{.ProjectName}})
    - Package.json has correct dependencies
  </verify>
  <done>
    Next.js frontend structure added to template with:
    - App Router layout and page
    - Tailwind CSS configured
    - TypeScript setup
    - API client pointing to backend
  </done>
</task>

<task type="auto">
  <name>Task 2: Add monorepo coordination files</name>
  <files>
    Template repo root:
    - package.json (root)
    - .gitignore (updated)
    - README.md (updated)
  </files>
  <action>
    1. Create root package.json:
       - name: "{{.ProjectName}}"
       - private: true
       - workspaces: ["frontend"]
       - scripts:
         - dev: "concurrently \"cd backend && go run ./cmd/server\" \"cd frontend && npm run dev\""
         - build: "cd frontend && npm run build"
         - install:frontend: "cd frontend && npm install"

    2. Update .gitignore:
       - Add Node patterns (node_modules/, .next/, .env.local)
       - Keep Go patterns

    3. Update README.md:
       - Document monorepo structure
       - Setup instructions:
         1. npm run install:frontend
         2. Copy .env.example files
         3. npm run dev
       - Explain frontend/backend communication
  </action>
  <verify>
    - Root package.json exists with workspace config
    - .gitignore covers both Node and Go
    - README has clear setup instructions
  </verify>
  <done>
    Monorepo coordination complete with dev scripts and documentation
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit and push template updates</name>
  <files>Template repository</files>
  <action>
    1. Stage all new frontend files
    2. Commit: "feat: add Next.js frontend structure"
    3. Push to GitHub
    4. Verify forge can still clone and process template:
       - go build ./cmd/forge
       - ./forge new test-web --stack web --features ""
       - Verify both backend/ and frontend/ exist with correct substitutions
       - Clean up test directory
  </action>
  <verify>
    - GitHub repo updated with frontend
    - forge new creates complete monorepo structure
    - Template variables substituted in both frontend and backend
  </verify>
  <done>
    Web stack template complete with both backend and frontend
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] frontend/ structure in template repo
- [ ] Next.js App Router with Tailwind
- [ ] Root package.json with monorepo scripts
- [ ] forge new creates working monorepo
- [ ] Template variables work in all files
</verification>

<success_criteria>
- Next.js frontend added to template
- Tailwind CSS configured
- API client set up to call backend
- Root-level dev scripts work
- Complete monorepo structure
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-stack-base/05-02-SUMMARY.md`

This completes Phase 5 (Web Stack Base). Update STATE.md and ROADMAP.md.
</output>
