---
phase: 05-web-stack-base
plan: 01
type: execute
---

<objective>
Create the web stack template repository with Go backend structure.

Purpose: Provide the foundational template that forge clones for web projects - starting with the Go backend which is consistent across web and mobile stacks.
Output: Git repository at github.com/bigbytes/forge-template-web with complete Go backend structure.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase context:
@.planning/phases/04-feature-registry/04-01-SUMMARY.md

# Stack configuration to understand template URL:
@internal/stacks/web.go

**Tech stack for template:**
- Go with Echo framework
- golang-standards/project-layout structure (cmd/, internal/, pkg/, api/)
- Supabase as database (connection configured, not implemented)

**Constraining decisions:**
- [PROJECT.md]: Arquitectura Frontend → Go → Supabase (backend as proxy)
- [PROJECT.md]: Echo framework for Go backend
- [PROJECT.md]: golang-standards/project-layout structure
- [03-01]: Template repos are cloned via go-git

**Current state:**
The CLI expects template at `https://github.com/bigbytes/forge-template-web` (see internal/stacks/web.go).
This template doesn't exist yet - this phase creates it.

**Important:** This phase creates an EXTERNAL template repository, not code in the forge CLI itself. The template will be a separate Git repo that forge clones.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template repository with Go backend structure</name>
  <files>
    Template repo structure:
    - backend/cmd/server/main.go
    - backend/internal/config/config.go
    - backend/internal/server/server.go
    - backend/internal/server/routes.go
    - backend/go.mod
    - backend/go.sum
    - backend/.env.example
    - README.md
  </files>
  <action>
    Create a new local directory for the template repository.

    1. Create backend/cmd/server/main.go:
       - Entry point that loads config and starts server
       - Use {{.ProjectName}} for module name in comments

    2. Create backend/internal/config/config.go:
       - Load environment variables (PORT, SUPABASE_URL, SUPABASE_KEY)
       - Use envconfig or simple os.Getenv pattern

    3. Create backend/internal/server/server.go:
       - Echo server setup with middleware (Logger, Recover, CORS)
       - Start method that binds routes

    4. Create backend/internal/server/routes.go:
       - Health check endpoint: GET /health
       - API group: /api/v1
       - Placeholder for future routes

    5. Create backend/go.mod:
       - Module: github.com/{{.ProjectName}}/backend
       - Dependencies: echo, godotenv

    6. Create backend/.env.example:
       - PORT=8080
       - SUPABASE_URL=
       - SUPABASE_KEY=

    7. Create README.md:
       - Project name: {{.ProjectName}}
       - Brief description of monorepo structure
       - Development instructions

    Use text/template syntax ({{.ProjectName}}, {{.StackType}}) for variable substitution.
    The template engine in forge will process these.
  </action>
  <verify>
    - Directory structure matches golang-standards/project-layout
    - All files contain proper template variables
    - go.mod has correct module path with template variable
  </verify>
  <done>
    Backend template structure created with:
    - Echo server with health check
    - Config loading from environment
    - Proper project-layout structure
    - Template variables for project name
  </done>
</task>

<task type="auto">
  <name>Task 2: Initialize Git repository and push to GitHub</name>
  <files>Template repository root</files>
  <action>
    1. Initialize Git repo in template directory
    2. Create .gitignore with Go patterns (*.exe, vendor/, .env)
    3. Create initial commit
    4. Create GitHub repository: bigbytes/forge-template-web
    5. Push to GitHub

    Use gh CLI for GitHub operations.
  </action>
  <verify>
    - gh repo view bigbytes/forge-template-web succeeds
    - Repository contains all template files
  </verify>
  <done>
    Template repository live at github.com/bigbytes/forge-template-web
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify forge can clone and process template</name>
  <files>None (verification only)</files>
  <action>
    1. Build forge: go build ./cmd/forge
    2. Test template cloning in a temp directory:
       ./forge new test-project --stack web --features ""
    3. Verify:
       - Template clones successfully
       - Variables are substituted ({{.ProjectName}} → test-project)
       - Backend structure is correct
    4. Clean up test directory
  </action>
  <verify>
    - forge new completes without errors
    - test-project/backend/go.mod contains "test-project"
    - test-project/backend/cmd/server/main.go exists
  </verify>
  <done>
    Forge successfully clones and processes web template
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Template repo exists at github.com/bigbytes/forge-template-web
- [ ] Backend follows golang-standards/project-layout
- [ ] Echo server with health check endpoint
- [ ] Template variables work ({{.ProjectName}} substituted)
- [ ] `forge new` successfully creates project from template
</verification>

<success_criteria>
- Template repository created and pushed to GitHub
- Go backend structure with Echo server
- Config loading from environment variables
- Health check endpoint at /health
- Template variables properly substituted by forge
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-stack-base/05-01-SUMMARY.md`

If this is the only plan for Phase 5, also update STATE.md and ROADMAP.md.
Otherwise, note readiness for 05-02-PLAN.md (Next.js frontend).
</output>
