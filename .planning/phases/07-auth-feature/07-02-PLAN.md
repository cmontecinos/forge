---
phase: 07-auth-feature
plan: 02
type: execute
---

<objective>
Add authentication hooks, context, and components to the Next.js frontend template.

Purpose: Provide complete auth UI and state management for web applications.
Output: Auth context, hooks, login/register components, and protected route wrapper.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auth-feature/07-01-SUMMARY.md

**Prior plan output:** Backend auth endpoints at /auth/register, /auth/login, /auth/refresh, /auth/logout

**Tech stack available:** Next.js 14, React, TypeScript, Tailwind CSS
**Established patterns:**
- API client at frontend/src/lib/api.ts
- App Router structure
- Environment variables via NEXT_PUBLIC_* prefix

**Template repo:** github.com/cmontecinos/forge (local: /tmp/forge-template-web)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth context and hooks</name>
  <files>/tmp/forge-template-web/frontend/src/lib/auth.ts, /tmp/forge-template-web/frontend/src/contexts/AuthContext.tsx, /tmp/forge-template-web/frontend/src/hooks/useAuth.ts</files>
  <action>
1. **src/lib/auth.ts** - Auth API functions:
   - Types: User, AuthResponse, LoginCredentials, RegisterCredentials
   - register(email, password) - POST /auth/register
   - login(email, password) - POST /auth/login
   - logout() - POST /auth/logout
   - refreshToken(refreshToken) - POST /auth/refresh
   - Token storage helpers (localStorage for access_token, refresh_token)

2. **src/contexts/AuthContext.tsx** - React context for auth state:
   - AuthContextType: user, isLoading, isAuthenticated, login, logout, register
   - AuthProvider component wrapping children
   - Load user from stored token on mount
   - Auto-refresh token before expiry (optional enhancement)
   - Handle auth errors (clear tokens, redirect to login)

3. **src/hooks/useAuth.ts** - Custom hook:
   - Export useAuth() hook that uses AuthContext
   - Throw helpful error if used outside AuthProvider

4. **Update src/app/layout.tsx** - Wrap with AuthProvider:
   - Import AuthProvider
   - Wrap {children} with <AuthProvider>
  </action>
  <verify>grep "AuthProvider" /tmp/forge-template-web/frontend/src/contexts/AuthContext.tsx</verify>
  <done>Auth context and hooks created, layout wrapped with AuthProvider</done>
</task>

<task type="auto">
  <name>Task 2: Create auth components and protected route</name>
  <files>/tmp/forge-template-web/frontend/src/components/auth/LoginForm.tsx, /tmp/forge-template-web/frontend/src/components/auth/RegisterForm.tsx, /tmp/forge-template-web/frontend/src/components/auth/ProtectedRoute.tsx, /tmp/forge-template-web/frontend/src/app/login/page.tsx, /tmp/forge-template-web/frontend/src/app/register/page.tsx</files>
  <action>
1. **src/components/auth/LoginForm.tsx** - Login form component:
   - Email and password inputs with Tailwind styling
   - Form validation (required fields, email format)
   - Submit handler calling useAuth().login()
   - Loading state during submission
   - Error display for failed login
   - Link to register page

2. **src/components/auth/RegisterForm.tsx** - Registration form:
   - Email, password, confirm password inputs
   - Form validation (password match, minimum length)
   - Submit handler calling useAuth().register()
   - Loading state, error display
   - Link to login page

3. **src/components/auth/ProtectedRoute.tsx** - Route protection wrapper:
   - Check isAuthenticated from useAuth()
   - Show loading spinner while checking auth
   - Redirect to /login if not authenticated
   - Render children if authenticated

4. **src/app/login/page.tsx** - Login page:
   - Simple page rendering LoginForm
   - Redirect to / if already authenticated

5. **src/app/register/page.tsx** - Register page:
   - Simple page rendering RegisterForm
   - Redirect to / if already authenticated

6. **Update src/app/page.tsx** - Show auth state:
   - Display logged-in user email
   - Logout button
   - Login/Register links if not authenticated

7. **Commit and push:**
   - git add, commit "feat: add auth components and pages"
   - git push to github.com/cmontecinos/forge
  </action>
  <verify>ls /tmp/forge-template-web/frontend/src/components/auth/ shows LoginForm.tsx, RegisterForm.tsx, ProtectedRoute.tsx</verify>
  <done>Auth components created, login/register pages added, template pushed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AuthContext and useAuth hook exist
- [ ] LoginForm and RegisterForm components exist
- [ ] ProtectedRoute wrapper exists
- [ ] /login and /register pages exist
- [ ] Template pushed to GitHub
</verification>

<success_criteria>

- All tasks completed
- Auth flow works: register → login → protected content → logout
- Components use Tailwind for styling
- Template repo updated
- Ready for 07-03-PLAN.md (mobile frontend auth)
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-feature/07-02-SUMMARY.md`
</output>
