---
phase: 07-auth-feature
plan: 03
type: execute
---

<objective>
Add authentication hooks, context, and components to the Expo mobile template.

Purpose: Provide complete auth UI and state management for mobile applications.
Output: Auth context, hooks, login/register screens, and navigation guards.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-auth-feature/07-01-SUMMARY.md
@.planning/phases/07-auth-feature/07-02-SUMMARY.md

**Prior plan output:** Backend auth endpoints, web frontend auth components

**Tech stack available:** Expo SDK 50, React Native, TypeScript, NativeWind
**Established patterns:**
- API client at mobile/src/lib/api.ts
- NativeWind for Tailwind-style styling
- Environment config via expo-constants

**Template repo:** github.com/cmontecinos/forge-mobile (local: /tmp/forge-template-mobile)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth context and hooks for mobile</name>
  <files>/tmp/forge-template-mobile/mobile/src/lib/auth.ts, /tmp/forge-template-mobile/mobile/src/contexts/AuthContext.tsx, /tmp/forge-template-mobile/mobile/src/hooks/useAuth.ts</files>
  <action>
1. **src/lib/auth.ts** - Auth API functions (similar to web):
   - Types: User, AuthResponse, LoginCredentials, RegisterCredentials
   - register(email, password) - POST /auth/register
   - login(email, password) - POST /auth/login
   - logout() - POST /auth/logout
   - refreshToken(refreshToken) - POST /auth/refresh
   - Token storage using expo-secure-store (not localStorage)
   - Add expo-secure-store to package.json dependencies

2. **src/contexts/AuthContext.tsx** - React context for auth state:
   - AuthContextType: user, isLoading, isAuthenticated, login, logout, register
   - AuthProvider component wrapping children
   - Load user from SecureStore on mount
   - Handle auth errors (clear tokens)

3. **src/hooks/useAuth.ts** - Custom hook:
   - Export useAuth() hook that uses AuthContext
   - Throw helpful error if used outside AuthProvider

4. **Update App.tsx** - Wrap with AuthProvider:
   - Import AuthProvider
   - Wrap content with <AuthProvider>

5. **Update package.json** - Add expo-secure-store:
   - "expo-secure-store": "~12.8.0"
  </action>
  <verify>grep "AuthProvider" /tmp/forge-template-mobile/mobile/src/contexts/AuthContext.tsx</verify>
  <done>Auth context and hooks created for mobile, using SecureStore for tokens</done>
</task>

<task type="auto">
  <name>Task 2: Create auth screens and navigation</name>
  <files>/tmp/forge-template-mobile/mobile/src/screens/LoginScreen.tsx, /tmp/forge-template-mobile/mobile/src/screens/RegisterScreen.tsx, /tmp/forge-template-mobile/mobile/src/screens/HomeScreen.tsx, /tmp/forge-template-mobile/mobile/src/navigation/AuthNavigator.tsx</files>
  <action>
1. **src/screens/LoginScreen.tsx** - Login screen:
   - Email and password TextInputs with NativeWind styling
   - Form validation (required fields)
   - Submit handler calling useAuth().login()
   - Loading state (ActivityIndicator)
   - Error display
   - Link to register screen

2. **src/screens/RegisterScreen.tsx** - Registration screen:
   - Email, password, confirm password inputs
   - Form validation (password match, minimum length)
   - Submit handler calling useAuth().register()
   - Loading state, error display
   - Link to login screen

3. **src/screens/HomeScreen.tsx** - Home screen (authenticated):
   - Display logged-in user email
   - Logout button
   - Backend health check status

4. **src/navigation/AuthNavigator.tsx** - Navigation with auth guards:
   - Use @react-navigation/native and @react-navigation/native-stack
   - If not authenticated: show Login/Register screens
   - If authenticated: show Home screen
   - Add navigation dependencies to package.json

5. **Update App.tsx** - Use AuthNavigator:
   - Import NavigationContainer and AuthNavigator
   - Render <NavigationContainer><AuthNavigator /></NavigationContainer>

6. **Update package.json** - Add navigation dependencies:
   - "@react-navigation/native": "^6.1.0"
   - "@react-navigation/native-stack": "^6.9.0"
   - "react-native-screens": "~3.29.0"
   - "react-native-safe-area-context": "4.8.2" (already present)

7. **Commit and push:**
   - git add, commit "feat: add auth screens and navigation"
   - git push to github.com/cmontecinos/forge-mobile
  </action>
  <verify>ls /tmp/forge-template-mobile/mobile/src/screens/ shows LoginScreen.tsx, RegisterScreen.tsx, HomeScreen.tsx</verify>
  <done>Auth screens created, navigation configured, template pushed</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] AuthContext and useAuth hook exist for mobile
- [ ] LoginScreen and RegisterScreen exist
- [ ] Navigation guards authentication state
- [ ] expo-secure-store used for token storage
- [ ] Template pushed to GitHub
</verification>

<success_criteria>

- All tasks completed
- Auth flow works on mobile: register → login → home → logout
- Screens use NativeWind styling
- Tokens stored securely with expo-secure-store
- Navigation responds to auth state
- Template repo updated
- Phase 7 complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-auth-feature/07-03-SUMMARY.md`
</output>
