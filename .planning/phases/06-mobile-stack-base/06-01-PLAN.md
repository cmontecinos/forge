---
phase: 06-mobile-stack-base
plan: 01
type: execute
---

<objective>
Create Expo React Native template structure for mobile stack.

Purpose: Provide the mobile/ frontend portion of the mobile stack template, following the same monorepo pattern as the web stack.
Output: Expo app template with TypeScript, NativeWind (Tailwind for RN), and API client for backend communication.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-stack-base/05-01-SUMMARY.md
@.planning/phases/05-web-stack-base/05-02-SUMMARY.md

**Tech stack available:** Go, Echo, godotenv, Next.js 14, Tailwind CSS, TypeScript
**Established patterns:**
- Template files use {{.ProjectName}} for variable substitution
- Backend follows cmd/internal structure
- Frontend calls backend via api.ts client
- Environment variables in .env.example files

**Constraining decisions:**
- [05-01]: Template repo at github.com/cmontecinos/forge (web stack)
- [05-01]: Echo v4 for backend HTTP framework
- [05-02]: API client pattern with environment variable for API URL
- [PROJECT]: Expo managed workflow for React Native

**Key insight:** Mobile template needs separate repo since template structure differs (mobile/ vs frontend/).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Expo mobile template structure</name>
  <files>/tmp/forge-template-mobile/mobile/package.json, /tmp/forge-template-mobile/mobile/app.json, /tmp/forge-template-mobile/mobile/App.tsx, /tmp/forge-template-mobile/mobile/tsconfig.json, /tmp/forge-template-mobile/mobile/.env.example, /tmp/forge-template-mobile/mobile/babel.config.js, /tmp/forge-template-mobile/mobile/tailwind.config.js, /tmp/forge-template-mobile/mobile/global.css</files>
  <action>
Create Expo managed workflow template at /tmp/forge-template-mobile/mobile/:

1. **package.json** - Expo SDK 50, React Native 0.73, NativeWind 4, TypeScript:
   - name: "mobile"
   - scripts: start, ios, android, web
   - dependencies: expo ~50.0.0, react-native, nativewind ^4.0, expo-status-bar
   - devDependencies: @types/react, typescript, tailwindcss

2. **app.json** - Expo config:
   - name: "{{.ProjectName}}"
   - slug: "{{.ProjectName}}"
   - version: "1.0.0"
   - orientation: portrait
   - scheme: "{{.ProjectName}}"
   - ios/android bundleIdentifier/package: "com.{{.ProjectName}}.app"

3. **App.tsx** - Entry point with NativeWind:
   - Import global.css for NativeWind
   - StatusBar component
   - SafeAreaView with centered content
   - Text showing "Welcome to {{.ProjectName}}"
   - Health check status display (using API client)

4. **tsconfig.json** - TypeScript config:
   - extends expo/tsconfig.base
   - compilerOptions: strict true, paths for @/* alias

5. **babel.config.js** - Babel with NativeWind preset

6. **tailwind.config.js** - Tailwind for NativeWind:
   - content: ["./App.{js,jsx,ts,tsx}", "./**/*.{js,jsx,ts,tsx}"]
   - presets: [require("nativewind/preset")]

7. **global.css** - Tailwind directives (@tailwind base/components/utilities)

8. **.env.example** - API_URL=http://localhost:8080

Use Expo SDK 50 (current stable). NativeWind 4 for Tailwind-style styling.
  </action>
  <verify>All files created with correct structure, package.json has valid JSON</verify>
  <done>Expo template structure complete with TypeScript, NativeWind, and environment config</done>
</task>

<task type="auto">
  <name>Task 2: Create mobile API client</name>
  <files>/tmp/forge-template-mobile/mobile/src/lib/api.ts, /tmp/forge-template-mobile/mobile/metro.config.js, /tmp/forge-template-mobile/mobile/nativewind-env.d.ts</files>
  <action>
1. **src/lib/api.ts** - API client mirroring web pattern:
   - Get API_URL from process.env or Constants.expoConfig.extra
   - checkHealth() function returning Promise<{status: string}>
   - Use fetch with proper error handling
   - Export typed functions for backend communication

2. **metro.config.js** - Metro bundler config:
   - Required for NativeWind CSS support
   - withNativeWind wrapper from nativewind/metro

3. **nativewind-env.d.ts** - TypeScript declaration:
   - Reference nativewind/types

Follow same API client pattern as web frontend (frontend/src/lib/api.ts).
  </action>
  <verify>cat /tmp/forge-template-mobile/mobile/src/lib/api.ts shows checkHealth function</verify>
  <done>API client created with health check function, Metro configured for NativeWind</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All template files exist in /tmp/forge-template-mobile/mobile/
- [ ] package.json is valid JSON with Expo SDK 50
- [ ] App.tsx uses NativeWind classes
- [ ] api.ts has checkHealth function
- [ ] Template variables {{.ProjectName}} used consistently
</verification>

<success_criteria>

- All tasks completed
- Expo template structure matches modern Expo patterns
- NativeWind configured for Tailwind-style styling
- API client follows same pattern as web frontend
- Ready for 06-02-PLAN.md (monorepo + CLI update)
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-stack-base/06-01-SUMMARY.md`
</output>
