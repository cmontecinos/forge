---
phase: 06-mobile-stack-base
plan: 02
type: execute
---

<objective>
Add monorepo coordination, Go backend (copied from web template), push to GitHub, and update CLI.

Purpose: Complete the mobile stack template and integrate it with the forge CLI.
Output: Working mobile template at github.com/cmontecinos/forge-mobile, forge CLI updated to use it.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-mobile-stack-base/06-01-SUMMARY.md

**Prior plan output:** Expo template at /tmp/forge-template-mobile/mobile/

**Tech stack available:** Go, Echo, godotenv, Expo SDK 50, NativeWind, TypeScript
**Established patterns:**
- Root package.json orchestrates monorepo
- README.md documents full stack
- Backend is identical across stacks (Go Echo)

**Constraining decisions:**
- [05-01]: Backend uses Echo v4, godotenv, cmd/internal structure
- [05-02]: concurrently for running both servers in dev
- [06-01]: Mobile uses Expo SDK 50, NativeWind

**Template repo:** github.com/cmontecinos/forge-mobile (new repo for mobile stack)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy Go backend and add monorepo files</name>
  <files>/tmp/forge-template-mobile/backend/*, /tmp/forge-template-mobile/package.json, /tmp/forge-template-mobile/README.md, /tmp/forge-template-mobile/.gitignore</files>
  <action>
1. **Copy backend/** from web template:
   - cp -r /tmp/forge-template-web/backend /tmp/forge-template-mobile/
   - Backend is identical for both stacks (Go Echo server)

2. **package.json** (root) - Monorepo coordination:
   - name: "{{.ProjectName}}"
   - version: "0.1.0"
   - private: true
   - workspaces: ["mobile"]
   - scripts:
     - dev: concurrently "cd backend && go run ./cmd/server" "cd mobile && npm start"
     - dev:mobile: cd mobile && npm start
     - dev:backend: cd backend && go run ./cmd/server
     - dev:ios: cd mobile && npm run ios
     - dev:android: cd mobile && npm run android
     - build:mobile: cd mobile && npx expo export
     - install:mobile: cd mobile && npm install
   - devDependencies: concurrently ^8.2.2

3. **README.md** - Mobile stack documentation:
   - Project structure (mobile/ + backend/)
   - Prerequisites (Node.js, Go, Expo CLI)
   - Setup instructions (npm install, env files)
   - Development commands (npm run dev, dev:ios, dev:android)
   - API endpoints table
   - Architecture diagram (App → Expo → Go API → Supabase)
   - Environment variables for both backend and mobile
   - Building for production

4. **.gitignore** - Combined Go + Node + Expo patterns:
   - Go: /bin, *.exe
   - Node: node_modules/, .env, .env.local
   - Expo: .expo/, dist/, *.jks, *.keystore
  </action>
  <verify>ls /tmp/forge-template-mobile/ shows backend/, mobile/, package.json, README.md</verify>
  <done>Complete monorepo structure with backend, mobile, and coordination files</done>
</task>

<task type="auto">
  <name>Task 2: Push to GitHub and update CLI</name>
  <files>internal/stacks/mobile.go</files>
  <action>
1. **Initialize and push template repo:**
   - cd /tmp/forge-template-mobile
   - git init
   - git add .
   - git commit -m "Initial mobile stack template"
   - Create repo: gh repo create cmontecinos/forge-mobile --public --source=. --push
   - If gh auth fails, user provides repo URL and push via SSH

2. **Update mobile.go** - Change template URL:
   - const MobileTemplateURL = "https://github.com/cmontecinos/forge-mobile"
   - Keep everything else the same (ID, Name, Description, TemplateConfig)

3. **Verify CLI builds:**
   - cd /Users/cmontecinos/Bigbytes/code_templates
   - go build ./cmd/forge
  </action>
  <verify>go build succeeds, grep MobileTemplateURL internal/stacks/mobile.go shows new URL</verify>
  <done>Template pushed to GitHub, CLI updated with new template URL</done>
</task>

<task type="auto">
  <name>Task 3: Verify forge creates complete mobile project</name>
  <files>N/A (verification only)</files>
  <action>
1. **Test template cloning:**
   - cd /tmp && rm -rf test-mobile-app
   - /Users/cmontecinos/Bigbytes/code_templates/forge new test-mobile-app --stack mobile --non-interactive

2. **Verify output structure:**
   - ls /tmp/test-mobile-app/ (should show mobile/, backend/, package.json, README.md)
   - cat /tmp/test-mobile-app/mobile/app.json | grep "test-mobile-app" (variable substitution)
   - cat /tmp/test-mobile-app/mobile/package.json (valid JSON)
   - cat /tmp/test-mobile-app/backend/go.mod | grep "test-mobile-app" (backend substitution)

3. **Verify no template artifacts:**
   - No {{.ProjectName}} remaining in any files
   - No .git directory (clean clone)
  </action>
  <verify>All verification commands pass, project structure complete</verify>
  <done>forge new creates complete mobile monorepo with Expo + Go backend</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Template repo exists at github.com/cmontecinos/forge-mobile
- [ ] internal/stacks/mobile.go has correct URL
- [ ] go build ./cmd/forge succeeds
- [ ] forge new --stack mobile creates complete project
- [ ] Variable substitution works ({{.ProjectName}} → actual name)
</verification>

<success_criteria>

- All tasks completed
- Mobile template deployed to GitHub
- CLI updated and working
- forge new --stack mobile creates usable project
- Phase 6 complete
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-stack-base/06-02-SUMMARY.md`
</output>
