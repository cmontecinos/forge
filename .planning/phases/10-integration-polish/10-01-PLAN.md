---
phase: 10-integration-polish
plan: 01
type: execute
---

<objective>
Add post-create dependency installation and E2E tests for the complete forge workflow.

Purpose: Ensure `forge new` creates fully ready-to-run projects with all dependencies installed.
Output: Post-create hooks running npm install and go mod tidy, E2E tests validating the flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior plan output:**
- Complete CLI with `forge new` command
- Web and mobile templates with auth, database, API features
- Templates cloned and processed correctly

**Tech stack available:** Go, Cobra CLI
**Established patterns:**
- CLI commands in internal/cli/
- Template processing in internal/templates/

**CLI location:** /Users/cmontecinos/Bigbytes/code_templates
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add post-create dependency installation</name>
  <files>internal/cli/new.go, internal/cli/postcreate.go</files>
  <action>
1. **Create internal/cli/postcreate.go** - Post-create hooks:
   - RunPostCreate(targetDir, stackType string) error
   - installFrontendDeps(dir string) error
     - Detect frontend/mobile directory
     - Run `npm install` or `yarn install`
     - Show spinner/progress
   - installBackendDeps(dir string) error
     - Detect backend directory
     - Run `go mod tidy`
     - Show progress
   - Handle errors gracefully (warn but don't fail)

2. **Update internal/cli/new.go** - Call post-create:
   - After ProcessTemplates and CleanupGitDir
   - Call RunPostCreate(targetDir, string(stack))
   - Add --skip-install flag to skip dependency installation
   - Show timing for install step

3. **Update PrintNextSteps** - Adjust based on install:
   - If deps installed: skip npm install / go mod tidy steps
   - Show "cd <project> && npm run dev" directly
  </action>
  <verify>grep "RunPostCreate" internal/cli/new.go</verify>
  <done>Post-create hooks install dependencies automatically</done>
</task>

<task type="auto">
  <name>Task 2: Add E2E tests for CLI</name>
  <files>internal/cli/new_test.go</files>
  <action>
1. **Create internal/cli/new_test.go** - E2E tests:
   - TestNewCommand_WebStack - Creates web project, verifies structure
   - TestNewCommand_MobileStack - Creates mobile project, verifies structure
   - TestNewCommand_InvalidName - Validates name rejection
   - TestNewCommand_ExistingDirectory - Verifies error handling
   - TestNewCommand_ForceOverwrite - Verifies --force flag

   Each test should:
   - Create temp directory
   - Run forge new with appropriate flags
   - Verify expected files exist
   - Verify template variables replaced
   - Clean up temp directory

2. **Add test helpers:**
   - runForgeNew(args ...string) (output string, err error)
   - assertFileExists(t, path string)
   - assertFileContains(t, path, content string)
   - createTempDir(t) string

3. **Run tests and fix any issues:**
   - go test ./internal/cli/... -v
  </action>
  <verify>go test ./internal/cli/... -v 2>&1 | head -50</verify>
  <done>E2E tests validate complete CLI workflow</done>
</task>

<task type="auto">
  <name>Task 3: Update README with usage documentation</name>
  <files>README.md</files>
  <action>
1. **Update README.md** with complete documentation:
   - Project description
   - Installation instructions (go install)
   - Quick start guide
   - Available stacks (web, mobile)
   - Available features (auth, database, api)
   - CLI usage examples
   - Project structure after creation
   - Development workflow
   - Contributing guidelines (basic)

2. **Commit all changes:**
   - git add all modified files
   - git commit with descriptive message
  </action>
  <verify>grep "forge new" README.md</verify>
  <done>README documents complete CLI usage</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Post-create installs npm and go dependencies
- [ ] --skip-install flag works
- [ ] E2E tests pass
- [ ] README has complete usage docs
- [ ] Full flow works: forge new my-app → ready to run
</verification>

<success_criteria>

- All tasks completed
- `forge new my-app --stack web` creates project with deps installed
- `forge new my-app --stack mobile` creates project with deps installed
- E2E tests validate the workflow
- README provides clear usage instructions
- Phase 10 complete - Forge v1.0 ready!
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-polish/10-01-SUMMARY.md`
</output>
