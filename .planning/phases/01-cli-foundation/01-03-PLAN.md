---
phase: 01-cli-foundation
plan: 03
type: execute
---

<objective>
Create `forge new` subcommand with argument parsing for project name.

Purpose: Implement the main entry point for project creation.
Output: Working `forge new <project-name>` command that validates input.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-02-SUMMARY.md

**From prior plans:**
- Cobra framework integrated
- Root command working
- Version command exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `forge new` subcommand skeleton</name>
  <files>internal/cli/new.go</files>
  <action>
    1. Create internal/cli/new.go with:
       - newCmd *cobra.Command
       - Use: "new <project-name>"
       - Short: "Create a new project"
       - Long: Description of what it does
       - Args: cobra.ExactArgs(1) to require project name
       - Run function that:
         a. Gets project name from args[0]
         b. Validates project name (alphanumeric, hyphens, underscores only)
         c. Checks if directory already exists
         d. Prints "Creating project: <name>" (placeholder for now)

    2. Add newCmd to rootCmd in init()

    Validation rules for project name:
    - Must start with letter
    - Only alphanumeric, hyphens, underscores
    - No spaces
    - Max 64 characters
  </action>
  <verify>
    go build ./cmd/forge && ./forge new my-project (prints "Creating project: my-project")
  </verify>
  <done>`forge new my-project` validates name and prints confirmation</done>
</task>

<task type="auto">
  <name>Task 2: Add output directory flag</name>
  <files>internal/cli/new.go</files>
  <action>
    1. Add --output or -o flag to newCmd:
       - Default: current directory
       - Description: "Output directory for the new project"

    2. Update Run function to:
       - Use output flag if provided, otherwise use project name as directory
       - Resolve to absolute path
       - Check if target directory exists (error if exists and not empty)
       - Print "Output: <absolute-path>"

    3. Add --force or -f flag:
       - If set, allow overwriting existing directory
       - Print warning when using force

    Error messages should be clear and actionable.
  </action>
  <verify>
    go build ./cmd/forge && ./forge new my-project -o /tmp/test-project (prints output path)
  </verify>
  <done>`forge new` accepts -o flag and validates output directory</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `forge new` requires exactly one argument
- [ ] `forge new my-project` validates name and shows output
- [ ] `forge new -o /path my-project` uses custom output
- [ ] Invalid project names show clear error
- [ ] Existing directory check works
</verification>

<success_criteria>
- `forge new` subcommand registered and working
- Project name validation implemented
- Output directory handling complete
- Ready for interactive prompts integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-03-SUMMARY.md`
</output>
