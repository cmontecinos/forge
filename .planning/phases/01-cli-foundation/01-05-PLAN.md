---
phase: 01-cli-foundation
plan: 05
type: execute
---

<objective>
Polish CLI with help text, error handling, and end-to-end verification.

Purpose: Ensure CLI is production-ready with good UX and proper error handling.
Output: Polished CLI ready for template engine integration in Phase 2.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-04-SUMMARY.md

**From prior plans:**
- Full CLI flow working
- Interactive prompts integrated
- Non-interactive flags available
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve help text and examples</name>
  <files>internal/cli/root.go, internal/cli/new.go</files>
  <action>
    1. Update rootCmd Long description with:
       - Clear explanation of what Forge does
       - Available stacks
       - Quick start example

    2. Update newCmd with:
       - Example usage in help:
         ```
         forge new my-app                    # Interactive mode
         forge new my-app --stack web        # Non-interactive stack selection
         forge new my-app --stack mobile --features auth,api
         ```

    3. Add annotations for help grouping if needed

    4. Ensure `forge help new` shows comprehensive help

    Help text should be scannable and useful.
  </action>
  <verify>
    go build ./cmd/forge && ./forge --help && ./forge help new
  </verify>
  <done>Help text is comprehensive with examples</done>
</task>

<task type="auto">
  <name>Task 2: Add error handling and colored output</name>
  <files>internal/cli/errors.go, internal/cli/output.go, internal/cli/new.go</files>
  <action>
    1. Create internal/cli/output.go with:
       - Add fatih/color: `go get github.com/fatih/color@latest`
       - Success(msg string) - green checkmark + message
       - Error(msg string) - red X + message
       - Info(msg string) - blue info + message
       - Warn(msg string) - yellow warning + message

    2. Create internal/cli/errors.go with:
       - Custom error types for common failures
       - ExitWithError(err error) - prints error and exits with code 1
       - Wrap errors with context

    3. Update new.go to use colored output:
       - Success for each completed step
       - Error for validation failures
       - Info for summary output

    4. Handle Ctrl+C gracefully:
       - Detect interrupt during prompts
       - Print "Aborted" and exit cleanly
  </action>
  <verify>
    go build ./cmd/forge && ./forge new invalid@name (shows colored error)
    go build ./cmd/forge && ./forge new valid-name --stack web --features auth (shows colored success)
  </verify>
  <done>Colored output works, errors are clear, Ctrl+C handled</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete CLI Foundation: forge new with interactive prompts, colored output, and help</what-built>
  <how-to-verify>
    1. Build: go build ./cmd/forge
    2. Test help: ./forge --help (should show description and commands)
    3. Test version: ./forge version (should show version info)
    4. Test new (interactive):
       ./forge new my-test-project
       - Select "Web" stack
       - Select "Auth" and "API" features
       - Verify colored summary appears
    5. Test new (non-interactive):
       ./forge new my-test-project --stack mobile --features auth,database,api
       - Should skip prompts and show summary
    6. Test error handling:
       ./forge new invalid@name (should show red error)
       ./forge new (missing name - should show error)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 1, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/forge` succeeds without warnings
- [ ] `forge --help` shows clear, helpful text
- [ ] `forge new --help` shows examples
- [ ] Interactive flow works with colored output
- [ ] Non-interactive flags work
- [ ] Errors show in red with clear messages
- [ ] Ctrl+C exits gracefully
</verification>

<success_criteria>
- CLI is polished and user-friendly
- All commands have good help text
- Error handling is consistent and clear
- Colored output improves UX
- Phase 1 complete, ready for Phase 2 (Template Engine)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-05-SUMMARY.md`

This is the final plan for Phase 1. After summary, Phase 1 is complete.
</output>
