---
phase: 01-cli-foundation
plan: 02
type: execute
---

<objective>
Integrate Cobra CLI framework with root command and basic configuration.

Purpose: Establish the CLI command structure that all subcommands will build upon.
Output: Working `forge` command with help, using Cobra framework.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-01-SUMMARY.md

**From prior plan:**
- Go module initialized
- Directory structure in place
- cmd/forge/main.go exists
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Cobra dependency and create root command</name>
  <files>go.mod, go.sum, internal/cli/root.go, cmd/forge/main.go</files>
  <action>
    1. Add Cobra: `go get github.com/spf13/cobra@latest`

    2. Create internal/cli/root.go with:
       - rootCmd *cobra.Command
       - Use: "forge"
       - Short: "Create projects with your preferred stacks"
       - Long: Description mentioning Next.js, Expo, Go, Supabase stacks
       - Execute() function that runs rootCmd.Execute()

    3. Update cmd/forge/main.go to call cli.Execute()

    Use Cobra best practices:
    - Keep rootCmd in internal/cli/ (not cmd/)
    - Use init() for flag binding
    - Handle errors in main.go
  </action>
  <verify>
    go build ./cmd/forge && ./forge --help (shows Cobra help output)
  </verify>
  <done>Cobra integrated, `forge --help` shows formatted help</done>
</task>

<task type="auto">
  <name>Task 2: Add version flag and command</name>
  <files>internal/cli/root.go, internal/cli/version.go, internal/config/version.go</files>
  <action>
    1. Create internal/config/version.go with:
       - Version = "0.1.0"
       - BuildDate (can be set at build time via ldflags)
       - GitCommit (can be set at build time via ldflags)

    2. Add --version flag to rootCmd that prints version info

    3. Create internal/cli/version.go with `forge version` subcommand:
       - Prints version, build date, git commit
       - Format: "forge version 0.1.0 (built: DATE, commit: HASH)"

    Both --version flag and version subcommand should work.
  </action>
  <verify>
    go build ./cmd/forge && ./forge --version && ./forge version
  </verify>
  <done>`forge --version` and `forge version` both print version info</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/forge` succeeds
- [ ] `forge --help` shows Cobra help with description
- [ ] `forge --version` prints version
- [ ] `forge version` prints detailed version info
</verification>

<success_criteria>
- Cobra framework properly integrated
- Root command configured with proper descriptions
- Version accessible via flag and subcommand
- Ready for `forge new` subcommand
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-02-SUMMARY.md`
</output>
