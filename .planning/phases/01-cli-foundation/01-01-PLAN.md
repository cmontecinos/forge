---
phase: 01-cli-foundation
plan: 01
type: execute
---

<objective>
Set up Go project structure following golang-standards/project-layout.

Purpose: Establish the foundation for Forge CLI with proper Go module and directory structure.
Output: Initialized Go module with standard project layout ready for CLI development.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech decisions from PROJECT.md:**
- CLI written in Go
- Follow golang-standards/project-layout structure
- Binary name: `forge`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and create project structure</name>
  <files>go.mod, cmd/forge/main.go, internal/cli/, internal/config/</files>
  <action>
    1. Run `go mod init github.com/bigbytes/forge` (or appropriate module path)
    2. Create directory structure:
       - cmd/forge/ (main entry point)
       - internal/cli/ (CLI commands)
       - internal/config/ (configuration)
       - internal/templates/ (template engine - future)
       - internal/stacks/ (stack registry - future)
       - internal/features/ (feature registry - future)
    3. Create minimal cmd/forge/main.go that just prints "forge cli" for now

    Follow golang-standards/project-layout conventions. Use internal/ for private packages.
  </action>
  <verify>
    go build ./cmd/forge && ./forge (prints "forge cli")
  </verify>
  <done>Go module initialized, directory structure created, binary builds and runs</done>
</task>

<task type="auto">
  <name>Task 2: Add .gitignore and initial documentation</name>
  <files>.gitignore, README.md</files>
  <action>
    1. Create .gitignore with Go-specific patterns:
       - Binary outputs (forge, *.exe)
       - IDE files (.idea/, .vscode/)
       - OS files (.DS_Store)
       - Test coverage files
       - Vendor directory (if used)

    2. Create minimal README.md with:
       - Project name and one-line description
       - Build instructions (`go build ./cmd/forge`)
       - Usage placeholder

    Keep README minimal - will be expanded in Phase 10.
  </action>
  <verify>
    cat .gitignore | grep -q "forge" && cat README.md | grep -q "Forge"
  </verify>
  <done>.gitignore covers Go artifacts, README has basic info</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go mod tidy` runs without errors
- [ ] `go build ./cmd/forge` produces binary
- [ ] Directory structure follows golang-standards layout
- [ ] .gitignore prevents binary from being tracked
</verification>

<success_criteria>
- Go module initialized with proper path
- Project structure matches golang-standards/project-layout
- Binary builds and runs
- Ready for Cobra CLI framework integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-01-SUMMARY.md`
</output>
