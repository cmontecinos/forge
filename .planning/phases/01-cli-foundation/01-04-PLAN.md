---
phase: 01-cli-foundation
plan: 04
type: execute
---

<objective>
Implement interactive prompts for stack and feature selection.

Purpose: Enable users to select stack type and features interactively.
Output: Interactive CLI flow: project name → stack selection → feature selection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-03-SUMMARY.md

**From prior plans:**
- `forge new <project-name>` command working
- Project name validation complete
- Output directory handling complete

**From PROJECT.md:**
- Two stacks: Web (Next.js + Go + Supabase), Mobile (Expo + Go + Supabase)
- Features: Auth, Database, API (optional, composable)
- User wants interactive selection
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add survey/promptui for interactive prompts</name>
  <files>go.mod, go.sum, internal/cli/prompts.go</files>
  <action>
    1. Add survey library: `go get github.com/AlecAivazis/survey/v2@latest`
       (survey is more user-friendly than promptui for multi-select)

    2. Create internal/cli/prompts.go with:
       - PromptStackSelection() returns selected stack string ("web" or "mobile")
         - Use survey.Select with options:
           - "Web (Next.js + Go + Supabase)"
           - "Mobile (Expo + Go + Supabase)"

       - PromptFeatureSelection() returns []string of selected features
         - Use survey.MultiSelect with options:
           - "Auth - Login/registro via backend"
           - "Database - Conexión Go-Supabase"
           - "API - Router, middlewares, handlers"
         - All can be selected, none required

    3. Define types for selections:
       - type StackType string (constants: StackWeb, StackMobile)
       - type Feature string (constants: FeatureAuth, FeatureDatabase, FeatureAPI)
  </action>
  <verify>
    go build ./cmd/forge (compiles with survey dependency)
  </verify>
  <done>Survey library added, prompt functions created with proper types</done>
</task>

<task type="auto">
  <name>Task 2: Integrate prompts into `forge new` flow</name>
  <files>internal/cli/new.go, internal/cli/prompts.go</files>
  <action>
    1. Update newCmd Run function to:
       a. Validate project name (existing)
       b. Call PromptStackSelection() → show stack selection
       c. Call PromptFeatureSelection() → show feature selection
       d. Print summary:
          "Creating project: <name>
           Stack: <web|mobile>
           Features: <auth, database, api> (or 'none')
           Output: <path>"

    2. Add --stack flag to allow non-interactive stack selection:
       - `--stack web` or `--stack mobile`
       - If provided, skip stack prompt

    3. Add --features flag for non-interactive feature selection:
       - `--features auth,database,api` (comma-separated)
       - If provided, skip features prompt

    Non-interactive flags enable scripting and automation.
  </action>
  <verify>
    go build ./cmd/forge && ./forge new test-project (interactive prompts appear)
    go build ./cmd/forge && ./forge new test-project --stack web --features auth,api (skips prompts)
  </verify>
  <done>Interactive flow works, non-interactive flags supported</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `forge new test` shows stack selection prompt
- [ ] After stack selection, feature selection appears
- [ ] After feature selection, summary is printed
- [ ] `forge new test --stack web` skips stack prompt
- [ ] `forge new test --features auth` skips features prompt
- [ ] Combined flags work: `forge new test --stack web --features auth,api`
</verification>

<success_criteria>
- Interactive prompts work smoothly
- User can select stack and features
- Non-interactive mode available via flags
- Summary shows all selections
- Ready for template engine integration
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-04-SUMMARY.md`
</output>
