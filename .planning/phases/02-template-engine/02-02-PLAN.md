---
phase: 02-template-engine
plan: 02
type: execute
---

<objective>
Integrate template engine with `forge new` command to create actual projects.

Purpose: Complete the template engine integration so users can generate projects from templates.
Output: `forge new` creates real project directories from Git templates with variable substitution.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-template-engine/02-01-SUMMARY.md

**From 02-01:**
- Template engine in internal/templates/
- CloneTemplate, ProcessTemplates, CleanupGitDir functions
- TemplateData, TemplateConfig types
- Error handling with TemplateError

**Key files:**
@internal/cli/new.go
@internal/cli/output.go
@internal/templates/engine.go
@internal/templates/config.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate template engine into forge new</name>
  <files>internal/cli/new.go, internal/templates/engine.go</files>
  <action>
    1. Update runNew() in new.go to:
       a. After collecting stack/features, call template engine:
          - Get template URL based on stack type (web/mobile)
          - Clone template to target directory
          - Process templates with TemplateData
          - Cleanup .git directory

       b. Show progress with colored output:
          - Info("Cloning template...")
          - Info("Processing templates...")
          - Success("Project created!")

       c. Handle errors gracefully:
          - If clone fails, show error and cleanup partial directory
          - If processing fails, show error but keep files for debugging

    2. Add CreateProject function in engine.go that orchestrates:
       - CreateProject(config TemplateConfig, data TemplateData, destDir string) error
       - Combines Clone → Process → Cleanup in sequence
       - Returns detailed error on any step failure

    3. Map stack type to template URL:
       - StackWeb → WebTemplateURL
       - StackMobile → MobileTemplateURL
  </action>
  <verify>
    go build ./cmd/forge
  </verify>
  <done>forge new calls template engine, shows progress, handles errors</done>
</task>

<task type="auto">
  <name>Task 2: Add project summary and next steps output</name>
  <files>internal/cli/new.go, internal/cli/output.go</files>
  <action>
    1. After successful project creation, print next steps:
       ```
       Project created successfully!

       Next steps:
         cd <project-name>
         # For web: npm install && npm run dev
         # For mobile: npm install && npx expo start
       ```

    2. Add PrintNextSteps(projectName string, stack StackType) in output.go

    3. Show creation time (optional but nice UX):
       - Track start time at beginning of runNew
       - Print "Created in X.Xs" at end
  </action>
  <verify>
    go build ./cmd/forge && ./forge new test-project --stack web --features auth (with placeholder template URL, will fail but shows flow)
  </verify>
  <done>Project creation shows progress and next steps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete template engine integrated with forge new command</what-built>
  <how-to-verify>
    1. Build: go build ./cmd/forge

    2. Test with a real Git repo (use any public template repo):
       - Create a simple test template repo with {{.ProjectName}} in files
       - Or use: https://github.com/bigbytes/forge-template-web (if exists)

    3. Run: ./forge new my-test-project --stack web --features auth
       - Should show "Cloning template..."
       - Should show "Processing templates..."
       - Should show "Project created!" or error if template URL not found

    4. If template URL not configured yet:
       - Verify the flow works (clone attempt, error handling)
       - We'll create actual templates in Phase 5/6

    5. Check error handling:
       - ./forge new test --stack web (with invalid URL should show clear error)
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 2, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/forge` succeeds
- [ ] forge new attempts to clone template
- [ ] Progress messages show during creation
- [ ] Errors handled gracefully with colored output
- [ ] Next steps printed after creation
</verification>

<success_criteria>
- Template engine fully integrated with CLI
- forge new creates projects from Git templates
- User sees progress during creation
- Error handling provides clear feedback
- Phase 2 complete, ready for Phase 3 (Stack Registry)
</success_criteria>

<output>
After completion, create `.planning/phases/02-template-engine/02-02-SUMMARY.md`

This is the final plan for Phase 2. After summary, Phase 2 is complete.
</output>
